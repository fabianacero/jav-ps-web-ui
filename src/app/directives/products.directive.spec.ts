import { ProductsDirective } from './products.directive';

describe('ProductsDirective', () => {
  it('should create an instance', () => {
    const directive = new ProductsDirective();
    expect(directive).toBeTruthy();
  });
});
